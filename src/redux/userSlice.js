import axios from "../utils/axios";


const initialState = {
  data: null,
  status: "idle",
  error: null,
};

export default function userReducer(state=initialState,action){
    switch(action.type){
        case "1":{
            return{
                
            }
        }
    }
}